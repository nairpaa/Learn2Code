fun main() {
    val names = arrayOf("Muhammad", "Aprian", "Nairpaa")

    var total = 0
    for (name in names) {
        println(name)
        total++
    }

    println("Total perulangan = $total")

    for (value in 0..100) {
        println(value)
    }
}